export type ModelListItemType = {
  id: number;
  thumbnail: string;
  description: string;
};

export type ModelType = {
  id: number;
  address1: string;
  address2: string;
  city: string;
  state: string;
  postal_code: string;
  thumbnail: string;
  model: string;
  description: string;
};
